# Input Rules

- `index.ts` registers shorthand syntax to insert grid tables via `$inputRule`.
- Uses command helpers from `../commands` and shared metadata utilities.
- Ensure new rules remain idempotent and covered by tests when behaviour changes.
- Refresh this note when command wiring or defaults change.
