# Remark Layer

- `wrapper.ts` exposes the Adobe grid tables remark plugin via `$remark`.
- `normalize-inline.ts` normalises inline markers inside grid-table cells.
- Ensure exports stay re-exported from package root (`../index.ts`).
- Update this guide whenever new remark transforms or options are added.
